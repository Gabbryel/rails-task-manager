<h1>TASKS</h1>

<% @tasks.each do |task| %>
<% if task.completed == true %>
<li><input type='checkbox' checked='true'> <%= link_to "#{task.title} | #{task.details}", task_path(task.id) %> <%= link_to "...🧨", task_path(task), method: :delete %> </li>
<% elsif task.completed == false %>
<li><input type='checkbox'> <%= link_to "#{task.title} | #{task.details}", task_path(task.id) %> <%= link_to "...🧨", task_path(task), method: :delete %> </li>
<% end%>
<% end %>

<%= link_to "+ new task", new_task_path %>